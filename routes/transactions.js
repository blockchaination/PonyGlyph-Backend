const express = require('express');
const router = express.Router();

// ✅ Placeholder for transaction handling
router.get('/', (req, res) => {
    res.json({ message: "🚀 Transactions API - Under Construction" });
});

module.exports = router;
